package nodes.helpers

import utils.helpers.UnitSpec

final class JsonDeserialiserSpec extends UnitSpec {

  "deserialize" should {
    "throws JsonValidationException when JSON is invalid" in pending /*{
      val j = mock[JsValue]
      a[JsonValidationException] should be thrownBy JsonDeserialiser.deserialize(j)
    }*/
  }
}