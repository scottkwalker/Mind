package fitness

import utils.helpers.UnitSpec

final class GeneratorSpec extends UnitSpec {

  "run" should {
    "get ai instance" in pending

    "return best solution and its fitness" in pending

    "loop the specified number of times" in pending

    "stop when threshold fitness reached" in pending
  }
}
